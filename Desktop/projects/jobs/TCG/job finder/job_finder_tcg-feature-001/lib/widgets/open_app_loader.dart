import 'package:flutter/material.dart';

class OpenAppLoader extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return (Container(
      alignment: Alignment.center,
      child: Text("loading"),
      color: Colors.white,
    ));
  }
}
